# 最終変更

更新日: 2026-04-30（aidiy_hermes 最大ステップ99変更）

## 直近の要約（aidiy_hermes 最大ステップ99変更）

- `aidiy_hermes` の通常 CLI 実行で使う最大ステップを 30 から 99 に変更した。
- `backend_hermes/cli_main.py` の `max_turns` 既定値と `AIAgent(max_iterations=...)` 指定を 99 に変更した。
- `backend_hermes/core/run_agent.py` の `AIAgent` 既定 `max_iterations` を 99 に変更した。
- 環境ループ側の既定値（`environments/agent_loop.py`、`hermes_swe_env.py`、`default.yaml`）も 99 に合わせた。
- 起動中の `aidiy_hermes` コマンドは `C:\Users\admin\Desktop\AiDiy2026 -c hermes\backend_hermes` を参照していたため、作業ルートと起動中コピーの両方へ同じ修正を入れた。
- 確認は対象 Python 4 ファイルの `py_compile` と、登録済み `uv tool` Python から `HermesCLI.max_turns` / `AIAgent.max_iterations` が 99 になる直接確認で実施した。

## 直近の要約（aidiy_hermes --version タイムアウト誤判定修正）

- AIコードパネルで `[aidiy_hermes]aidiy_hermesが利用できません。（aidiy_hermes未インストール?)` と出る原因を確認した。
- 実体は未インストールではなく、`aidiy_hermes.exe --version` が cold start / 複数チャンネル同時接続時に 10 秒を超えてタイムアウトし、未導入扱いになっていた。
- `backend_server/AIコア/AIコード_cli.py` の `バージョン確認()` に CLI 別キャッシュと `asyncio.Lock` を追加し、同時接続時も `--version` を1回だけ実行するようにした。
- `aidiy_hermes` だけバージョン確認タイムアウトを 30 秒へ延長した。ほかの CLI は従来どおり 10 秒。
- 起動中の実体が `C:\Users\admin\Desktop\AiDiy2026 -c hermes` 側だったため、同じ修正を作業ルートと起動中コピーの両方へ適用した。
- 確認は `py_compile` と、`aidiy_hermes` の `CodeAI.開始()` を4インスタンス同時実行する直接テストで実施し、全件 `aidiy_hermes v0.5.1` で成功した。

## 直近の要約（_cleanup.py temp/backup整理修正）

- `_cleanup.py` の `backend_server/temp` / `backend_mcp/temp` 整理を「中身削除」からフォルダごとの削除へ変更した。
- `backend_hermes/temp` もクリーンアップ選択項目に追加し、フォルダごと削除するようにした。
- ルート `backup/` の削除選択を `_cleanup.py` に追加し、存在する場合は既定 `y` で削除確認して一括実行の最初に削除するようにした。
- docs 配下の `_cleanup.py` サンプルも、`temp` はフォルダごと削除する文言・実装へ合わせた。
- 確認は `python -m py_compile _cleanup.py` と、一時ディレクトリ上で `cleanup_backend()` / `cleanup_backend_mcp()` / `cleanup_backend_hermes()` / `cleanup_backup()` を直接呼ぶ削除挙動テストで実施した。
- 関連知見は `.aidiy/knowledge/クリーンアップ手順.md` に追加した。

## 直近の要約（backend_hermes execute_code 出力デコード修正）

- `backend_hermes/tools/code_execution_tool.py` の `subprocess.run(text=True)` に `encoding="utf-8"` / `errors="replace"` を明示し、Windows 既定 `cp932` で UTF-8 出力を読んで `UnicodeDecodeError` になる経路を潰した。
- 子 Python には `PYTHONIOENCODING=utf-8:replace` を渡し、通常の `print()` 出力も UTF-8 として親側で安定して読めるようにした。
- stdout / stderr を `_normalize_output()` で正規化し、`None` や `bytes`、timeout の部分出力でも JSON 化前に安全な文字列へ変換するようにした。
- これにより reader thread が落ちた後に `proc.stdout[-20000:]` で `TypeError: 'NoneType' object is not subscriptable` になる二次エラーも防ぐ。
- 確認は `backend_hermes/tools/code_execution_tool.py` の `py_compile`、MCP の Python 構文チェック、`execute_code` 直接呼び出しで日本語出力・不正バイト列・timeout 部分出力を検証した。
- 関連知見は `.aidiy/knowledge/CodeCLI表示ANSI制御コード対処.md` に追記した。

## 直近の要約（backend_hermes 統合に伴う Markdown 追従）

- 追補として、`backend_server` / `frontend_web` に残っていた旧公開名 `hermes_cli` と旧設定キー `CODE_HERMES_CLI_MODEL` の参照を整理し、現行の `aidiy_hermes` / `CODE_AIDIY_HERMES_MODEL` を正とするよう補正した。
- ただし旧セッション状態や旧フロントから送られる値を壊さないため、サーバー側では旧キー・旧AI名を受け取った際に現行名へ正規化する互換処理を残した。
- ルートの `README.md` / `AGENTS.md` / `CLAUDE.md` / `_AIDIY.md` を見直し、`backend_hermes` を「常駐サーバーではない統合 CLI 基盤」として明記した。
- Code AI 名の表記を `hermes_cli` から現行実装の `aidiy_hermes` へ統一し、`CODE_AIDIY_HERMES_MODEL` を併記した。
- `backend_server/AGENTS.md` は `aidiy_hermes` の統合方法に合わせて更新し、専用 `AiDiy_code_*.json` ではなく `conf_model.py` の動的モデル生成を正とした。
- `backend_hermes/README.md` / `backend_hermes/AGENTS.md` は、古い「Ollama 専用」「`hermes_cli` で連携」説明をやめ、現在の `aidiy_hermes` / `--provider` 対応 / on-demand CLI 前提へ全面更新した。
- `.aidiy/knowledge` では、Markdown 追従観点・Code CLI 追加手順・バックエンド起動メモを更新し、`_start.py` が Hermes を起動しない点を明記した。

## 直近の要約（backend_hermes 深掘りリファクタリング）

- 旧版 `_old_hermes-agent` を参照しながら、ルート shim 削除後に残っていた `run_agent` / `cli` / `hermes_state` 旧 import 経路を整理した。
- `core/run_agent.py` を旧版ツール互換に寄せ、`delegate_task` が参照する `platform`、provider 制御、progress callback、session DB、memory store、activity summary などの軽量属性を持たせた。
- `base/model_tools.py` の `memory` / `session_search` / `delegate_task` 特別停止を外し、必要な agent 状態を `registry.dispatch()` へ渡す形に一本化した。
- `tools/delegate_tool.py`、`hermes_cli/oneshot.py`、`core/transports/codex.py`、`hermes_cli/callbacks.py` の旧トップレベル import を現行構成へ修正した。
- `hermes_state.py` を軽量互換 DB として追加し、`session_search` / dashboard 系が参照する `SessionDB` API の最低限を復活した。
- `pyproject.toml` の配布対象へ `hermes_state`、`environments`、`plugins` を追加し、`requirements.txt` / `pyproject.toml` に不足していた `rich` を追加した。
- Windows で `plugin.yaml` を cp932 として読んで `disk-cleanup/plugin.yaml` が parse 失敗する問題を、manifest 読み込みを `encoding="utf-8-sig"` に統一して修正した。
- AiDiy 表示で `?[32m` のように ANSI 色制御が見える問題に対し、`cli_main.py` の色判定を `_supports_ansi_color()` に集約し、明確な ANSI 対応端末以外では `_strip_ansi()` で除去するようにした。
- 旧版にあったステップ数・実行中ステップ表示を復活し、`core/run_agent.py` から `thinking_callback` / `step_callback` / `tool_progress_callback` で進捗を通知、`cli_main.py` の spinner 行へ `Step 1/99: モデル呼び出し中` / `Step 1/99: ツール実行中 ...` と表示するようにした。
- ツール完了ごとに実行時間を計測し、stderr へ `done Step n/max: <tool> (0.1s)` 形式で順次出力するようにした。TUI の一時表示とは別の実行履歴として残る。
- 確認は 267 Python ファイルの `compile()`、registry 重複 AST 検査、`pyproject.toml` parse、`disk-cleanup/plugin.yaml` parse、ANSI 判定 helper、`SessionDB`、`memory` dispatch で実施した。グローバル Python には `prompt_toolkit` / `requests` / `httpx` / `websockets` が未導入のため、フル CLI 起動は依存導入環境での確認が必要。

## 直近の要約（backend_hermes registry 重複整理）

- ユーザー要望「backend_hermes リファクタリング。必要に応じて旧も確認。ステップを細かく設定し、順に実施」に合わせ、旧版 `_old_hermes-agent` の役割を確認しながら低リスク単位で整理した。
- `backend_hermes/pyproject.toml` が消えていたため復元し、`py-modules = ["cli_main"]` と `base` package 構成に合わせた。
- `todo` / `clarify` は AiDiy 用の軽量 `planning_tools.py` を registry 正本にし、旧版由来の `todo_tool.py` / `clarify_tool.py` は直接 import 用の関数・クラスだけ残して自動登録を止めた。
- `image_generate` / `vision_analyze` / `process` も registry 重複があったため、正本を `media_tools.py` / `vision_tool.py` / `process_tool.py` に寄せ、旧版由来の `image_generation_tool.py` / `vision_tools.py` / `process_registry.py` は補助・直接 import 用として残した。
- 現行モジュールが直接 import する `httpx[socks]`、`python-dotenv`、`pydantic` を `requirements.txt` と `pyproject.toml` に追加し、`AGENTS.md` と `README.md` の依存説明を更新した。
- 生成された `__pycache__` と検証用 `temp/state/todo.json` は削除した。
- 確認は旧トップレベル import 検索、registry 登録名重複の AST 検査、266 Python ファイルの構文 `compile()`、`tomllib` による `pyproject.toml` 検証、軽量 `todo` / `clarify` の dispatch 確認で実施した。`.venv` が存在しないため、依存導入後の `cli_main.py --help` は未実行。

## 直近の要約（backend_hermes base import 統一）

## 直近の要約（backend_hermes base import 統一）

- ユーザー要望「backend_hermes 直下の `utils.py` 等が `base` 内とダブっている」に合わせ、共通モジュールの実体を `backend_hermes/base/` に一本化した。
- `backend_hermes/utils.py`、`hermes_constants.py`、`hermes_logging.py`、`hermes_time.py`、`model_tools.py`、`toolsets.py` の旧 import 互換 shim を削除した。
- `core/`、`hermes_cli/`、`tools/`、`plugins/`、`environments/` の旧トップレベル import を `from base...` へ置換した。`model_tools` をモジュール変数として使う箇所は `from base import model_tools` にした。
- `backend_hermes/pyproject.toml` は `py-modules = ["cli_main"]` のみとし、共通モジュールは `base` package として扱う。
- `backend_hermes/README.md` の構成例と import 例を現行構成へ更新した。
- 確認は `_old_hermes-agent` / `skills` / `optional-skills` を除いた旧 import 検索と、対象 Python 全ファイルの `py_compile` で実施した。CLI 実行確認はグローバル Python に `prompt_toolkit` / `openai` 等が未導入のため依存不足で未完了。

## 直近の要約（backend_hermes TUI出力色調整）

## 直近の要約（backend_hermes TUI出力色調整）

- ユーザー要望「backend_hermes治してるんですよ」に合わせ、対象を `frontend_avatar` ではなく `backend_hermes/cli_main.py` に絞って修正した。
- 下部固定入力欄の上下罫線 `input-rule` を白 (`#FFFFFF`) に変更した。スキン上書きで戻らないよう `_build_tui_style_dict()` の最後でも再適用する。
- 入力後にスクロールバックへ流れるユーザー入力を `_print_user_message()` 経由に変更し、上下線で挟む表示にした。
- ただしスクロールバック出力へ ANSI 色コードを直接入れると AiDiy 表示で `?[32m` のように見えるため、ANSI は出さない方針に修正した。
- モデル応答は `_print_cpu_output()` で、`CPU OUTPUT` ではなく `AiDiy` 表記に変更した。通常ターミナル（TTY）では緑色、AiDiyアプリ経由など非TTYではANSIなしにして文字化けを防ぐ。
- スクロールバックの横線は ASCII `-` ではなく `─` に変更し、非TTY時は stdout/stderr を UTF-8 に reconfigure して AiDiy 側で文字化けしないようにした。
- 途中経過・待機表示の `hint` をシアン/青系 (`#00BFFF italic`) に変更し、スキン上書き後も維持する。
- 確認は `backend_hermes/cli_main.py` の `py_compile`、`--help`、`_build_tui_style_dict()` と表示 helper の直接確認で実施した。helper 出力に ANSI が混ざらないことも確認した。
- 関連知見は `.aidiy/knowledge/HermesCLI_TUI調整手順.md` に追記した。

## 直近の要約（CodeCLI表示ANSI制御コード対処）

## 直近の要約（CodeCLI表示ANSI制御コード対処）

- ユーザー要望「メッセージ表示 色記号おかしい」に合わせ、AiDiy の回答に `?[36m` / `?[0m` のような ANSI 色制御コードが見える問題を修正した。
- `backend_server/AIコア/AIコード_cli.py` に ANSI 除去処理を追加し、CLI subprocess の stdout / stderr を WebSocket へ送信する前にプレーンテキスト化するようにした。
- subprocess 環境変数に `NO_COLOR=1`、`FORCE_COLOR=0`、`CLICOLOR=0`、`TERM=dumb` を渡し、CLI 側にも色なし出力を促すようにした。
- 実 ESC 文字の ANSI だけでなく、表示経路で壊れた `?[36m` 形式も除去対象にした。
- `frontend_avatar/src/components/AIコード.vue` と `frontend_avatar/src/components/AIチャット.vue` の `受信内容文字列()` でも ANSI 除去を行い、既存履歴や別経路から混ざった色記号も表示直前で落とすようにした。
- 確認は `backend_server/AIコア/AIコード_cli.py` の `py_compile`、`frontend_avatar` の `npm run type-check`、ANSI除去関数の直接確認で実施した。
- 関連知見は `.aidiy/knowledge/CodeCLI表示ANSI制御コード対処.md` に追加した。

## 直近の要約（backend_hermes TUI旧版寄せ）

## 直近の要約（backend_hermes TUI旧版寄せ）

- ユーザー要望「TUIの動きが違う。メソッドをサイドバイサイド的に確認」に合わせ、`backend_hermes/_old_hermes-agent/cli.py` と `backend_hermes/cli_main.py` の TUI メソッドを横比較した。
- 最大差分は、現行 `cli_main.py` が `out_buf` / `out_win` を TUI レイアウト内に持っていた点。旧版は通常 stdout + `patch_stdout()` で会話出力をスクロールバックへ流し、TUI は下部固定入力だけを持つ。
- `cli_main.py` の `_build_tui()` を旧版寄せし、`out_buf` / `out_win` を削除した。レイアウトは `Window(height=0)` → `model_picker` → `spinner` → `spacer` → `status_bar` → `input_rule_top` → `input_area` → `input_rule_bot` → `CompletionsMenu` の順へ変更した。
- `CompletionsMenu(max_height=12, scroll_offset=1)`、`mouse_support=False`、`patch_stdout()`、resize 時の renderer reset + screen clear を追加し、旧版の表示崩れ対策に寄せた。
- `Ctrl+C` は入力クリア / 実行中 interrupt / 2回目 force exit に寄せ、`Ctrl+D` は readline 的な delete-or-exit、`Ctrl+L` は full redraw、`Ctrl+Z` は Windows 非対応メッセージを出す形にした。
- `_force_full_redraw()`、`_get_tui_terminal_width()`、`_use_minimal_tui_chrome()`、`_tui_input_rule_height()`、`_agent_spacer_height()`、`_spinner_widget_height()`、`_get_status_bar_fragments()`、`_build_tui_layout_children()` など旧版 TUI 補助メソッドを復活した。
- `backend_hermes/旧新の実装対比表.md` に「14. TUI メソッド横比較・旧版寄せ」を追記した。
- 確認は `cli_main.py` の `py_compile`、`--version`、`--help`、`--list-tools`、231ファイル `py_compile` で実施。非対話環境での `_build_tui()` 直接生成は Windows コンソールがないため既知の `NoConsoleScreenBufferError`。
- 追加のソースレベル確認で、旧版 TUI 主要メソッド 24 件に対し現行側の欠落が 0 になるよう、`_get_status_bar_snapshot`、`_get_tui_prompt_symbols`、`_status_bar_context_style`、`_build_context_bar`、`_command_spinner_frame` を追加した。
- `text.startswith("/")` だけの slash 判定を旧版寄せし、`_looks_like_slash_command()`、`_strip_leaked_bracketed_paste_wrappers()`、`_strip_leaked_terminal_responses()` を追加した。
- 旧版の prompt symbol `❯` は cp932 出力環境で `UnicodeEncodeError` になる可能性があるため、`_fits_output_encoding()` を追加し、現在の stdout encoding で出せない場合は `>>> ` へ fallback する。
- 追加で「色合い・点滅具合」をソースレベルで横比較し、旧版 / 新版の `_tui_style_base` が 41 key で完全一致することを確認した。欠けていた `image-badge`、`clarify-*`、`sudo-*`、`approval-*`、`voice-*` 系の色を追加した。
- `_build_tui_style_dict()` は旧版同様に `hermes_cli.skin_engine.get_prompt_toolkit_style_overrides()` を重ねる形へ戻し、`_apply_tui_skin_style()` も復活した。
- slash command 実行中のスピナーは旧版同様 `int(time.monotonic() * 10)` の 10fps 相当に変更した。ただし Windows cp932 で旧版の点字フレームが出せない場合は ASCII フレームへ fallback する。
- 通常 spinner text は旧版同様にフレームを前置しない形へ戻し、経過秒表示も `60s` 以上は `Xm Ys` 表示へ合わせた。
- `spinner_loop` を追加し、コマンド実行中または spinner 表示中は旧版同様 `0.1s` 間隔で invalidate、idle は `0.2s` 待機にした。
- 再確認は旧新 TUI 主要メソッド 26 件の欠落 0、style key 差分 0、`cli_main.py py_compile`、231ファイル `py_compile`、`--help` で実施した。

## 直近の要約（backend_hermes 旧新実装比較・Windows対応）

- `backend_hermes/旧新の実装対比表.md` に「2026-04-29 追加実比較・実装対応」を追記した。
- `browser_tool.py` が参照していた `tools/browser_providers/` が新版側に欠落していたため、旧版から `base.py` / `browserbase.py` / `browser_use.py` / `firecrawl.py` / `__init__.py` を移植し、主要コメントを日本語化した。
- `tools/environments/local.py` に `_find_shell`、`_sanitize_subprocess_env`、`_HERMES_PROVIDER_ENV_BLOCKLIST` を復活し、`process_registry.py` の import 前提を満たした。Windows は Git Bash → PowerShell → cmd.exe の順で fallback する。
- `tools/terminal_tool.py` に `set_approval_callback` 互換 API を戻し、`delegate_tool.py` の import エラーを解消した。
- 旧 import パス互換のため、ルートに `hermes_constants.py` / `hermes_logging.py` / `hermes_time.py` / `model_tools.py` / `toolsets.py` / `utils.py` の `base.*` 再エクスポート shim を追加し、`pyproject.toml` の `py-modules` に登録した。
- チャンネル系は戻さない方針に合わせ、`hermes_cli/gateway.py` は安全な無効化スタブへ置換し、`tools/yuanbao_tools.py` は登録名を残しつつ常に利用不可にした。
- `requests` / `websockets` / `fal-client` を `requirements.txt` と `pyproject.toml` に追加し、`.venv` へ `uv pip install -r requirements.txt` で反映した。
- 確認は 231 ファイルの `py_compile`、`cli_main.py --version`、`--help`、`--list-tools`、`terminal_handler({'command': 'echo hermes_windows_ok'})`、`_find_shell()`、Yuanbao 無効判定で実施した。`--list-tools` は 49 件。

## 直近の要約（aidiy_hermes 旧コアツール復活追加）

- `backend_hermes/tools/vision_tool.py` を追加し、`openai_key_id` を使う `vision_analyze` を復活した。
- `backend_hermes/tools/planning_tools.py` を追加し、`todo` と `clarify` を軽量実装で復活した。`todo` は `backend_hermes/temp/state/todo.json` へ保存する。
- `backend_hermes/tools/code_execution_tool.py` を追加し、短い Python snippet を一時ファイルで実行する `execute_code` を復活した。
- `backend_hermes/tools/process_tool.py` を追加し、ローカルプロセスの一覧/kill を行う `process` を復活した。
- `base/model_tools.py` の `_AGENT_LOOP_TOOLS` から `todo` を外し、registry dispatch されるようにした。
- 先に追加した `tts_tool.py` / `media_tools.py` のスキーマが registry の期待する raw function schema ではなく nested schema になっていたため修正した。
- `--list-tools` は 15 件になった。`hermes-harness` 既定ツールセットにも 15 件が入ることを確認した。
- 確認は tools 全体の `py_compile`、`--list-tools`、`get_tool_definitions()`、`execute_code` / `todo` / `clarify` の直接 dispatch で実施した。実 API 呼び出しや ffmpeg 実動画作成はコスト・環境依存があるため未実行。

## 直近の要約（aidiy_hermes TTS/画像/動画ツール復活）

- `/model` が会話応答後に `[command] /model` として遅れて見える問題を避けるため、TUI Enter ハンドラで slash command を out buffer に積まないようにした。
- エージェント実行中に slash command を入力した場合は、警告後に入力バッファを reset して、後続ターンで残留コマンドが実行されないようにした。
- `backend_hermes/tools/tts_tool.py` を追加し、`AiDiy_key.json` の `openai_key_id` を使う `text_to_speech` ツールを復活した。出力先は既定で `backend_hermes/temp/tts/`。
- `backend_hermes/tools/media_tools.py` を追加し、`image_generate` と `create_video_from_images` を登録した。画像生成は OpenAI image API、動画作成は ffmpeg による画像列 MP4 化。
- `backend_hermes/base/toolsets.py` と `backend_hermes/base/model_tools.py` に `video` / `video_tools` を追加し、`hermes-harness` 既定ツールセットに `text_to_speech`、`image_generate`、`create_video_from_images` が入ることを確認した。
- 確認は `py_compile`、`--list-tools`、`get_tool_definitions()`、空引数時の `tool_error` で実施した。実 API 呼び出しはコストが発生するため未実行。

## 直近の要約（aidiy_hermes provider/model picker対応）

- `backend_hermes/cli_main.py` の `/model` を旧 Hermes 準拠の provider -> model 二段 picker へ寄せた。
- TUI では `/model` 引数なしで provider 一覧を開き、Enter で provider 選択、次に model 選択へ進む。上下キー、Back、Cancel、Esc/Ctrl+C キャンセルに対応した。
- TUI 外では `/model` で provider 一覧と直接指定例を表示する。直接指定は `/model <model> --provider <provider>` と `/model <provider>:<model>` を受け付ける。
- `aidiy_key.json` の有効キーから provider を構築するようにした。対応 provider は `ollama`、`openai`、`openrt`、`gemini`、`freeai`、`claude`。
- `openai` / `openrt` / `ollama` / `gemini` / `freeai` は OpenAI 互換 Chat Completions として扱う。`gemini` / `freeai` は Gemini OpenAI 互換エンドポイント `https://generativelanguage.googleapis.com/v1beta/openai` を使う。
- `claude` は OpenAI SDK 互換ではないため、`backend_hermes/core/run_agent.py` に Anthropic Messages API への最小変換を追加した。OpenAI 形式の tool 定義と tool result を Anthropic 形式へ変換する。
- モデル一覧は provider ごとの `/models` から取得し、取得できるものは `YYYY/MM/DD - model` の日付手前表示にした。Ollama Cloud は従来どおり `:cloud` / `:clude` を外す。
- `--provider` CLI 引数も追加した。例: `aidiy_hermes --provider openai -m gpt-5.2`。
- 確認は `py_compile`、`--help`、provider 一覧取得、`/model gemini:gemini-2.5-flash`、`/model claude-sonnet-4-6 --provider claude` の直接切替で実施した。

## 直近の要約（backend_hermes 旧Hermes TUI寄せ直し）

- ユーザー要望に合わせ、`backend_hermes/cli_main.py` の現行実装を先に `backup/manual/20260429_175108/backend_hermes_cli_main.py` へ手動バックアップした。
- 旧版 `_old_hermes-agent/cli.py` を基準に、`TextArea`、`FileHistory`、`SlashCommandCompleter`、`SlashCommandAutoSuggest`、Enter/Tab/上下キー/Alt+Enter/Ctrl+Enter の TUI 骨格を AiDiy Hermes 側へ寄せ直した。
- 旧版丸ごとコピーでは `rich` / `fire` / `agent.*` / gateway / cron など未導入依存が復活して壊れるため、TUI とコマンド処理の形をコピーし、現在の `core.run_agent.AIAgent` に接続する軽量実装に整理した。
- `/` 入力直後の補完、Tab 補完、上/下矢印履歴、複数行入力、`patch_stdout` 経由の実行中出力を入れた。
- `/new`、`/model`、`/help`、`/clear`、`/redraw`、`/history`、`/retry`、`/undo`、`/save`、`/copy`、`/tools`、`/toolsets`、`/config`、`/status`、`/exit` を実装し、補完表示は AiDiy Hermes 対応済みコマンドに絞った。
- `/model` の一覧は OpenAI 互換 `/models` から `created` を読み、`YYYY/MM/DD - model` の日付手前表示にした。Ollama Cloud は 240 日以内に絞って新しい順に並べる。
- Windows コンソールで行が落ちないよう、実行時表示の絵文字・罫線・点字スピナー依存を外し、ASCII 寄りの表示へ寄せた。
- 確認は `backend_hermes/.venv/Scripts/python.exe -m py_compile backend_hermes/cli_main.py`、`--help`、`--list-tools`、`process_command()` 直接確認で実施した。MCP code check は `Transport closed` で使えなかった。

## 直近の要約（backend_hermes TUI 操作感調整）

- `backend_hermes/cli_main.py` に `AiDiy HERMES` の ANSI 起動ロゴを追加し、`show_banner()` を簡潔なロゴ＋モデル＋endpoint 表示へ変更した。
- `TextArea` の `/` キーバインドを追加し、行頭コマンドとして `/` を入力したときだけ `start_completion(select_first=False)` を呼ぶようにして、スラッシュコマンドメニューを開きやすくした。
- `process_command()` を `resolve_command()` ベースに寄せ、`/new`、`/model <name>`、`/help`、`/clear`、`/tools`、`/status`、`/exit` を実装した。`/q` は registry の `queue` alias より優先して終了ショートカットとして扱う。
- `/new` は会話履歴、セッションID、スピナー状態、エージェントインスタンスをリセットする。`/model <name>` はセッション内のモデルを切り替え、次ターンで `AIAgent` を作り直す。
- `/model` 単体では現在モデル、endpoint、履歴件数に加え、OpenAI 互換 `/models` から取得できたモデルを新しい順で最大 12 件表示する。
- Windows `--help` が cp932 で落ちないよう、argparse の説明文から非 ASCII ダッシュを外した。
- 構文確認は `backend_hermes/.venv/Scripts/python.exe -m py_compile cli_main.py`、`--version`、`--help`、`/q`・`/model`・`/new` の直接メソッド確認で実施した。非コンソール環境では `_build_tui()` が `NoConsoleScreenBufferError` になるため、TUI 実操作は通常ターミナルで確認する。
- 関連知見は `.aidiy/knowledge/HermesCLI_TUI調整手順.md` に追加した。

## 前回の要約（Ollama Chat Cloud 直接接続対応）

- `backend_server/AIコア/AIチャット_ollama.py` で `ollama_key_id` が `<` で始まる場合は従来どおりローカル `ollama_host + "/v1"`、実キーの場合は `https://ollama.com/v1` を使うよう分岐した。
- Cloud 直叩き時は `CHAT_OLLAMA_MODEL` の `:cloud` と、入力揺れ対策の `:clude` を外して API に渡す。ローカル経由では従来どおりサフィックス付きモデル名を維持する。
- `backend_server/conf/conf_json.py` で `AiDiy_key.json` 読込時に `ollama_key_id` が有効なら、`CHAT_OLLAMA_MODEL`（存在すれば `OLLAMA_MODEL` も）の読取値だけ `:cloud` を除去するようにした。`AiDiy_key.json` 自体は変更しない。
- `backend_server/conf/conf_model.py` の Ollama モデル一覧取得を、実キーがある場合は `GET https://ollama.com/v1/models`、プレースホルダーの場合は `ollama list` に切り替えた。
- 追加で、Ollama のモデル表示値を他のチャットモデルと同じ `yyyy/mm/dd - model` 形式へ揃えた。Cloud は `created` timestamp を日付化し、ローカルは `yyyy/mm/dd` を使う。
- `backend_server/AIコア/AIチャット.py` の welcome 事前チェックで、`ollama_chat` は API キーがプレースホルダーでもローカル実行可能として無効扱いしないようにした。
- 実設定で Cloud モデル一覧 39 件取得を確認し、返却サンプルが `('deepseek-v4-flash', '2026/04/24 - deepseek-v4-flash')` の形になることを確認した。
- `py_compile` は変更 3 ファイルで成功。`ruff` は `backend_server` の venv に未インストールだったため実行できなかった。
- 関連知見は `.aidiy/knowledge/AIモデル設定変更手順.md` に統合済み。

## 直近の要約（X動画再生BGM 生状態表示追加）

- `frontend_web/public/X動画再生BGM/index.js` を `index_old.js` ベースのシンプル実装へ寄せた。
- `watchdog`、10秒タイムアウト復旧、失敗側復旧 helper、シーク待ちタイマーなどの追加制御を外し、旧版同様の `PLAYING` → `seekTo()` → 通常クロスフェードの流れに戻した。
- 今の UI に必要な差分として、外部 `playlist.js`、2分再生設定、音量セレクトだけを残している。
- ただし現在のブラウザ挙動では `seekTo()` 直後に停止しやすいため、旧版寄せを維持したまま `seekTo()` 後の 1 回だけ `playVideo()` を入れている。
- その後、プレイリスト項目クリックで次曲が始まらなかったため、`triggerCrossfade()` が指定曲を受け取れるようにし、`li.onclick` から選択曲を次側へ載せるよう修正した。
- さらに、プレイリストはシャッフル表示へ変更し、右側に `NOW PLAYING` カードを追加して再生中の曲名を大きく表示するようにした。スクロールしやすいよう `min-height: 0`、`margin/padding`、スクロールバー幅も調整した。
- 追加で、プレイリストを `playlist-drum` 枠で包み、上下フェード付きのドラム型スクロールへ調整した。右パネル高さも広げて、少なくとも 3 曲以上が見える前提にしている。
- さらに、各再生シーケンス開始時に **10 秒後に 1 回だけ判定**する形へ整理し、その時点で `PLAYING` でなければ、プレイリストクリックと**同じ `startSelectedVideoSequence()` 経路**で次曲シーケンスへ進むようにした。待機中の次側がある場合も、その枠を差し替える。
- 初回起動も別処理をやめ、`startInitialPlayback()` から `startSelectedVideoSequence()` を呼ぶ形へ統一した。これで初回もプレイリストクリック時と同じ `triggerCrossfade('選曲', video)` 経路を通る。
- 初期状態の「現在側」は `B` に寄せ、見た目も `player-b` を初期選択にそろえた。起動時はシャッフル済みプレイリストの 1 曲目を `startSelectedVideoSequence()` へ流し、**B 側が現在・A 側へ 1 曲目を載せる** クリック相当の動きにしている。
- 緑色のステータスは自前の「再生中」表示だったため、`status-text` は `getPlayerState()` の生値をそのまま `A:<state> B:<state> active:<side>` 形式で出すよう変更した。
- その後の切り分けで、**`1=PLAYING` / `2=PAUSED`** が正しく、`A:2` は「再生中」ではなく「停止/一時停止」を意味することを確認した。
- あわせて、初回 `PLAYING` 直後の `seekTo()` 前に成功扱いしていたため、`seekTo()` 後に `PAUSED` へ落ちると 10 秒後判定が空振りしやすかった。`seekTo()` を伴う場合は **再開後の `PLAYING` を確認してから** 初回確定・フェード開始・再生タイマー開始を進めるよう修正した。
- さらに、10 秒後の次曲開始が効かない原因として、**失敗した pending フェード状態のまま次シーケンスを上書きしていた** 点を修正した。判定失敗時は `pendingFade / fadeToPlayer / isCrossfading / currentVideo[side]` をいったんリセットしてから、新しい選曲シーケンスを開始する。
- あわせて `loadVideoById()` 後にも **1 回だけ `playVideo()` を明示実行** するようにし、初回ロード時と 10 秒後の再投入時の双方で、ブラウザ任せの自動開始に依存しすぎない形へ寄せた。
- テストしやすいよう、`X動画再生BGM` の既定切替時間は **2分→1分** に変更した。対象は `PLAY_DURATION_MS` のみで、再生シーケンスやフェード秒数は変えていない。
- 追加で、各再生シーケンスごとに **5 秒後の 1 回判定** を入れ、対象側が `2(PAUSED)` のままなら `playVideo()` を **1 回だけ** 再送するようにした。初回限定ではなく、その後の曲切替でも同じ補助が入る。10 秒後の次曲切替判定はそのまま残している。
- さらに 5 秒後補助は `playVideo()` 再送ではなく、**対象 iframe の中央を 1 回だけ疑似クリック** する形へ差し替えた。ユーザー操作に近い経路での復帰を優先している。
- 1 分切替に合わせ、`index.html` の初期表示時間も `1:00` / `残 1:00` に更新した。
- 再生時間は固定値ではなく UI 選択式へ変更し、`1分 / 5分 / 10分 / 20分 / 30分` を切り替えられるようにした。`PLAY_DURATION_MS` は可変にし、変更時は表示だけでなく現在の再生タイマーも再計算して即時反映する。

## 前回の要約（X動画再生BGM 初回シーク起動修正）

- `frontend_web/public/X動画再生BGM/index.js` の `MAX_VOLUME` を `const` から可変値へ修正した。
- 画面起動時に `DOMContentLoaded` 内で音量セレクト初期値を代入していたため、`Assignment to constant variable` で後続の YouTube IFrame API 読み込みまで止まっていた。
- 初期値は UI の既定値に合わせて `50` に揃えた。

## 前回の要約（X動画再生BGM 初回再生修正）

- `frontend_web/public/X動画再生BGM/` の初回再生を、2曲目以降と同じ `triggerCrossfade()` 経路へ統一した。
- 起動時は空のB側からA側へ切り替える扱いにし、A/B左右表示とクロスフェードの見た目を維持する。
- ブラウザの自動再生制限があるため、初回はミュート自動再生を試し、音声開始はオーバーレイのクリックで行う。
- ちらつき対策として、A/Bの両プレイヤーは左右に見せたまま、切替時だけ opacity を変える。

## 前回の要約（X世界の絶景 リスト拡充）

- `frontend_web/public/X世界の絶景/list.js` の絶景地点を 114 件から 184 件へ拡充した。
- 追加時は既存タイトルとの重複を避け、世界遺産級・国立公園級・地形として見栄えする場所に絞る。
- `wikiTitle` は `encodeURIComponent()` 前提なので、`%27` などの URL エンコード済み文字列を入れない。
- 確認は `node --check .\public\X世界の絶景\list.js`、重複チェック、ブラウザで `window.sceneryPlaces.length` を見る。

## 前回の要約（X世界の絶景 名称変更）

- 旧名称を `X世界の絶景` に変更した。
- 対象は `frontend_web/src/router/index.ts`、`frontend_web/src/components/Xその他.vue`、`frontend_web/src/components/Xその他/X世界の絶景.vue`、`frontend_web/public/X世界の絶景/`。
- 絶景地点リストは `frontend_web/public/X世界の絶景/list.js` に分離し、`index.html` では `list.js` を `index.js` より先に読み込む。
- 静的 JS の確認は `node --check .\public\X世界の絶景\list.js` と `node --check .\public\X世界の絶景\index.js`、Vue 側は `npm run type-check` を使う。

## 前回の要約（ナレッジ見直し修正）

- 2026-04-26 に `.aidiy/knowledge` 加筆後の見直しで 4 件を修正した。
  - **`backendMCP構成.md`** 行11: 存在しない `tools/__init__.py` への言及を削除し「`mcp_main.py` からは」に修正。
  - **`バックエンド起動.md`** 注意点セクション: `python -c "import apps_main"` の説明が「import エラーのデバッグ」セクションと重複していたため、注意点側の1行を削除。
  - **`CodeCLIプロンプト整形.md`** `_システムプロンプト構築()` のコード例: `suffix` 変数のコンテキストが不明だったため「OS 判定後に設定される文字列変数」旨のコメントを追加。
  - **`V系エンドポイント追加手順.md`** 基本構造の import 例: 実際のルーター（`V商品.py`）と異なる `from core_crud.auth import get_current_user` / `from apps_main import get_db` パターンを、実際の `import deps`・`deps.get_db`・`deps.get_現在利用者`・`apps_schema`・`apps_models` パターンへ修正。

## 前回の要約（ナレッジ詳細加筆）

- 2026-04-26 に `.aidiy/knowledge` の全ファイルを並行して詳細加筆した。
  - **`backendMCP構成.md`**: FastMCP インスタンス定義パターン・再起動ウォッチャー仕組み・Chrome 自動起動 (`_ensure_chrome()`)・PostgreSQL 遅延初期化を追加。
  - **`バックエンド起動.md`**: `_start.py` の 4 つの対話プロンプト（mcp/core+apps/Web/Avatar、デフォルト YYYn）・静止待機仕様・`startup_event()` 実行順序・import エラーデバッグ手順を追加。
  - **`Viteプロキシ設定.md`**: `ws: true` の意味（HTTP + WebSocket 両方をプロキシ）・`frontend_avatar` の `vite.config.ts`（`strictPort: true`）を追加。
  - **`CodeCLIプロンプト整形.md`**: `_CLI送信用テキスト正規化()` と `_コマンド構築()` の実装パターンをコード例付きで追加。
  - **`VRM_VRMA追加手順.md`**: `config.ts` の実際の定数値（`DEFAULT_VRM_MODEL_URL`・SAMPLE/STANDARD_VRMA_FILES）・`desktop:list-vrma-files` IPC の戻り値型を追加。
  - **`V系エンドポイント追加手順.md`**: `list_controls.py` の正確な場所（`backend_server/list_controls.py`、`apps_crud/` 配下ではない）・各関数のシグネチャを追加。
  - **`Docker構成.md`**: `docker_1build.bat` がフロントビルドを含む旨・マウント構成表・ポート構成表・コンテナ名 `aidiy2026`・HTTPS 自己署名証明書の説明を追加。
  - **`S系スケジューラ追加手順.md`**: `checkForUpdates()` の Vue 実装パターン（30 秒 setInterval・JWT 延長・初回差分判定）・ドラッグ座標計算方針を追加。

## 前回（aidiy_backup_save の stdio 中継修正）

- 2026-04-26 に `aidiy_backup_save` の MCP ツール呼び出し失敗を確認し、`backend_mcp/mcp_stdio.py` を修正した。
  - 直接実行では `BackupSave().diff_scan()` が 0.13 秒程度で成功したため、path 解決ではなく stdio/SSE 中継側を調査。
  - ログに `mcp.types.RequestParams.Meta() argument after ** must be a mapping, not Meta` が出ていたため、`req.params.meta` を `model_dump(by_alias=True, exclude_none=True)` 相当で `dict` 化してから `ClientSession.call_tool()` へ渡すようにした。
  - patched bridge 経由で `backup_diff_scan` と `backup_run` を確認済み。最終確認では直接 `backup_run` が `backup/20260426/150410` に差分 1 件（`backend_mcp/mcp_stdio.py`）を保存し、その後の直接 `diff_scan` は `count=0`。
  - 既存の Codex MCP 接続は古い stdio プロセスが閉じた状態になることがあるため、反映には MCP クライアント側の再接続または再起動が必要。

## 直近の変更ファイル（今回）

- `.aidiy/knowledge/backendMCP構成.md`
- `.aidiy/knowledge/_最終変更.md`
- `backend_mcp/mcp_stdio.py`

---

## 前回（2026-04-26 一斉整理）

- 2026-04-26 に `.aidiy/knowledge` をサブエージェントでカテゴリ別に一斉整理した。Backend、AI/Avatar/Code CLI、Frontend/認証/共通に分担し、作業ログ寄りの記述を再利用できる手順・注意点へ圧縮した。
- `_index.md` は各メモの現状に合わせて更新済み。本文側で未整理だった項目は、実装結論または次回追記観点へ整理済み。
- `ナレッジ更新手順.md` を追加し、`.aidiy/knowledge` の並列加筆時の分担、親エージェントによる `_index.md` / `_最終変更.md` 統合、リンク・BOM・古い表現の検証、MCPバックアップ失敗時の代替確認を明文化した。
- `Markdown現状追従チェック.md` に `.aidiy/knowledge` 横断チェック用の PowerShell 検証観点を追記した。`MCP活用手順.md` には `backup_run` タイムアウトや `Transport closed` 時の運用を追記した。
- `frontend_avatar` の表示選択まわりで `xeyes(目)`、`xneko(猫)`、アナログ時計、デジタル時計、カレンダー表示が追加・整理された。
- `AIコア_ネコ.vue` は `AIコア_xneko.vue` にリネーム済み。旧名参照が残りやすいので、変更時は `rg` と `frontend_avatar` の型チェックを実施する。
- `xneko_chatora.gif` / `xneko_mike.gif` は既存 `oneko.gif` の 32px タイル配置を維持した `256x128` 透過スプライトシート。色違い作成時は輪郭・透明部分を保持し、色だけ置換する方が安全。
- `xeyes` は Electron では main process の `screen.getCursorScreenPoint()` と `BrowserWindow.getBounds()` を返す IPC でウィンドウ外カーソルを追う。CPU使用率連動は main process の `os.cpus()` 差分を IPC で返し、renderer 側で移動平均を持つ。
- `frontend_web` には `X立体リバーシ` と `X世界の絶景` が追加済み。静的 X系画面を増やす場合は、本体 `public/<画面名>/`、iframe ラッパー、`router/index.ts`、`Xその他.vue` の4点を揃える。
- `X立体リバーシ` は 6面×4x4 の Three.js 立方体を直接クリックする構成。自動カメラ、チャレンジモード、CPUトグル、ルール説明の細かい注意点は `X系静的画面追加.md` に統合済み。
- `X世界の絶景` は Leaflet + OpenStreetMap + Wikipedia/Wikimedia の絶景巡回アプリ。外部ネットワーク依存、NOTICE、タイル読み込み待ち、2枚 map による先読み表示の注意点は `X系静的画面追加.md` に統合済み。
- `Xインベーダー` と `Xテトリス` のゲームループ・タイマー・多重実行対策も `X系静的画面追加.md` に統合済み。
- 2026-04-25 の .md 大規模追従整理では、MCP 8サーバー構成、M取引先/M取引先分類、認証延長、OpenRouterキー名、Avatar Storage、MCP実装ドキュメント分離などを現行実装へ合わせた。確認観点は `Markdown現状追従チェック.md` に統合済み。

## 直近の変更ファイル

- `frontend_avatar/src/components/AIコア.vue`
- `frontend_avatar/src/components/AIコア_xeyes.vue`
- `frontend_avatar/src/components/AIコア_xneko.vue`
- `frontend_avatar/src/components/AIコア_アバター.vue`
- `frontend_avatar/src/components/AIコア_アナログ時計.vue`
- `frontend_avatar/src/components/AIコア_デジタル時計.vue`
- `frontend_avatar/src/components/AIコア_カレンダー.vue`
- `frontend_avatar/electron/main.ts`
- `frontend_avatar/electron/preload.ts`
- `frontend_avatar/src/env.d.ts`
- `frontend_avatar/public/xneko_chatora.gif`
- `frontend_avatar/public/xneko_mike.gif`
- `frontend_web/src/router/index.ts`
- `frontend_web/src/components/Xその他.vue`
- `frontend_web/src/components/Xその他/X立体リバーシ.vue`
- `frontend_web/src/components/Xその他/X世界の絶景.vue`
- `frontend_web/public/X立体リバーシ/`
- `frontend_web/public/X世界の絶景/`
- `.aidiy/knowledge/X系静的画面追加.md`
- `.aidiy/knowledge/Markdown現状追従チェック.md`
- `.aidiy/knowledge/アバター表示とVRMA.md`

## 確認済み事項

- `frontend_web/public/X立体リバーシ/index.js` で `node --check` 実行済み
- `frontend_web/public/X世界の絶景/index.js` で `node --check` 実行済み
- `frontend_web` で `npm run type-check` 実行済み
- `frontend_avatar` で `npm run type-check` 実行済み
- `http://localhost:8090/X%E4%B8%96%E7%95%8C%E3%81%AE%E7%B5%B6%E6%99%AF/index.html` の HTTP 200 を確認済み

## 次回の運用

- `AIコア` のアバター固有設定は `AIコア_アバター.vue` 内へ置き、`xneko(猫)` / `xeyes(目)` / 時計 / カレンダー / `無し` 選択時には表示しない。
- `表示選択` を増やす場合は、独立コンポーネントを作って `AIコア.vue` の import、表示選択型、option、表示 computed を揃える。
- X系静的ゲームを増やす場合は `X系静的画面追加.md` を先に確認する。
- .md 全体を現行実装へ追従させる場合は `Markdown現状追従チェック.md` の検索コマンドと確認観点を使う。
