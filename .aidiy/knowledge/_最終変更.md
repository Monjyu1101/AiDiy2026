# 最終変更

更新日: 2026-04-25

## 今回整理した内容

- `frontend_web/public/X立体リバーシ/index.js` の自動カメラワークを調整した
  - 反転対象面が現在の視点で見えていない場合だけカメラ移動を積むようにした
  - 見えている反転はカメラ移動を挟まず、その場で反転モーションを再生するようにした
  - 右面/左面/上面/下面のカメラ目標角を補正し、反対面を映す向き間違いを修正した
  - チャレンジモード突入時、大きな突入メッセージを3Dビュー中央へ表示し、中央24枚を順に点滅・消去する演出を追加した
  - 中央24枚の消去時も、対象面が見えていない場合はカメラを向けてから消すようにした
  - チャレンジモード突入判定を直前モーション終了後に遅らせ、突入メッセージを透過表示にした
  - チャレンジモード中の合法手は中央24マスへ限定しつつ、反転判定は通常モードと同じに戻した
  - 着手コマを消去の逆モーションで表示し、見えていない面への着手時にもカメラを向けるようにした
  - ルール欄を「立体リバーシルール」として、ゲームルールが分かる説明へ書き直した
  - 青側もCPU実行へ切り替えられるよう、青CPUトグルと色別CPU判定を追加した
- `frontend_web/public/Xインベーダー/index.js` のゲームロジックを見直した
  - 敵全滅後の次ウェーブ待ち中に `nextWave()` が毎フレーム走り、WAVE が一気に増える不具合を修正した
  - `requestAnimationFrame` の多重起動を避ける `startLoop()` / `stopLoop()` を追加した
  - 固定フレーム前提だった移動・弾・タイマーを `dt` ベースへ補正した
  - 敵の端到達時に座標をクランプし、端でめり込み/揺れが出にくいようにした
  - 被弾時の敵弾クリアと短い無敵時間を追加し、ライフが連続で減りにくいようにした
  - 敵弾は各列の下側にいる敵を優先して撃つようにした
  - 射撃クールダウンと連射パワーアップのタイマーを管理し、新規開始・メニュー・ゲームオーバーで残留しないようにした
  - ポーズ中はスペースキーで弾を撃てないようにした
- `frontend_web/public/Xテトリス/index.js` のゲームロジックを見直した
  - ピース固定後、次ピース出現まで入力・落下・二重固定が起きないよう `pieceLocked` を追加した
  - 次ピース出現などのゲーム進行タイマーを `setManagedTimeout()` / `clearPendingTimers()` で管理するようにした
  - 複数ライン消去時に行インデックスがずれる削除処理を、残行再構成方式へ修正した
  - I ミノが縦回転できるようにした
  - ライン消去アニメーションが `renderBoard()` で即消えないよう、描画後に clearing クラスを付ける順序へ変更した
- `frontend_web` に `Xハローワールド` を追加した
  - `frontend_web/public/Xハローワールド/` に Leaflet + OpenStreetMap の絶景ランダム巡回アプリを追加した
  - `src/components/Xその他/Xハローワールド.vue` を全画面 iframe ラッパーとして追加した
  - `/Xその他/Xハローワールド/表示` ルートと `Xその他.vue` のメニュー導線を追加した
  - ランダム絶景ジャンプ、前へ、自動巡回、巡回進捗バー、訪問履歴を実装した
  - Wikipedia / Wikimedia Commons のサマリー画像を絶景写真として地図上に重ねるようにした
  - `NOTICE.md` に Leaflet / OpenStreetMap / Wikipedia / Wikimedia Commons 利用元と外部ネットワーク依存を記録した
  - 写真表示時の地図レイヤーを薄くし、自動巡回間隔を 24 秒へ変更した
  - Xその他メニューから `Xハローワールド/index.html` を別タブで直接開くようにした
  - 静的アプリ側にブラウザ全画面切替ボタンを追加した
  - 絶景地点を114件へ増やし、ランダム巡回をシャッフル済みキュー方式にして同じ地点へ偏りにくくした
  - 訪問履歴ログを右下の直近3件表示へ変更し、写真クレジットは右上へ移動した
  - 場所名表示を画面端近くまで使う1行表示にし、地図転換中バナーへ行き先と進行バーを追加した
  - 地図転換開始時に写真モードを解除して地図を明るく戻し、転換バナーを明るい配色へ変更した
  - 地図転換バナーのラベル文字を外し、写真は地図移動完了後にフェードインするよう遅延表示へ変更した
  - 地図表示中は場所名に国名を含め、写真表示後は場所名だけに戻すようにした
  - 地図転換中の画面上部バナー表示を削除した
  - 地図移動とタイル読み込み完了を待ってから次のズームや写真フェードへ進むようにした
  - 地図表示を2枚の Leaflet map 構成に変更し、裏側で目的地タイルを先読みしてから読み込み済み地図をフェードイン拡大する方式へ変更した
  - `.aidiy/knowledge/X系静的画面追加.md` に X立体リバーシと Xハローワールドの実装知見を追記した

## 前回整理した内容

- `frontend_web` に `X立体リバーシ` を追加した
  - `frontend_web/public/X立体リバーシ/` に 6面立方体リバーシ本体（HTML/CSS/JS）を新規作成した
  - `frontend_web/src/components/Xその他/X立体リバーシ.vue` を iframe ラッパーとして追加した
  - `frontend_web/src/router/index.ts` に `/Xその他/X立体リバーシ/ゲーム` を追加し、`Xその他.vue` にメニューカードを追加した
  - 4x4 の6面盤、斜めは同一面のみ、縦横は立方体面またぎ、チャレンジモード、CPU対戦、合法手表示、ルール明記を実装した
  - Three.js CDN ES module による 3D キューブ表示、ドラッグ自由回転、3D立方体上のクリック着手、着手後の低速反転アニメーション、自動カメラワークを追加した
  - 6面ネット表示は外し、3Dキューブ上で直接プレイする構成にした
- `frontend_avatar` の `表示選択` に `xeyes(目)` を追加した
  - `AIコア_xeyes.vue` を新規作成し、Web では画面内 pointermove、Electron では OS カーソル位置を使って目線を制御する
  - Electron main/preload/env に `getWindowPointerSnapshot(role)` を追加し、自分のウィンドウ role を渡して `bounds` と `mouse` を取得できるようにした
  - Electron main/preload/env に `getSystemCpuUsage()` を追加し、`AIコア_xeyes.vue` で 1 秒ごとの直近 10 秒 CPU 平均に応じて血走り演出を行うようにした
  - ボタン表示中だけ、xeyes の目の下に直近 10 秒 CPU 使用率グラフと平均値を表示するようにした
- `AIコア_デジタル時計.vue` の 7 セグメント参照を安全参照にして、型チェックが通るようにした
- `frontend_avatar` の `AIコア_ネコ.vue` を `AIコア_xneko.vue` へリネームした
  - `AIコア.vue` の import も新ファイル名へ更新した
  - `AIコア_xneko.vue` のスプライト参照に undefined ガードを追加し、型チェックを通した
- `AIコア.vue` の実験的アバター設定 UI を調整した
  - `アバター表示` チェックボックスを `表示選択` select に変更し、`アバター` / `xneko(猫)` / `無し` の 3 択にした
  - `不完全な自立身体制御` / `不完全な自動カメラワーク` / カメラモードは `AIコア_アバター.vue` 内へ移動した
  - `AIコア.vue` から `controls-visible` を渡し、ボタン非表示時はアバター内の設定 UI・字幕・発光を隠すようにした
- `表示選択` に `アナログ時計` / `デジタル時計` / `カレンダー` を追加した
  - 各表示は `AIコア_アナログ時計.vue` / `AIコア_デジタル時計.vue` / `AIコア_カレンダー.vue` の独立コンポーネントとして実装した
- `.aidiy/knowledge/アバター表示調整とVRMA再生の連続化.md` を更新し、表示選択と実験的設定 UI の配置を追記した

## 今回の変更ファイル

- `frontend_web/src/router/index.ts`
- `frontend_web/src/components/Xその他.vue`
- `frontend_web/src/components/Xその他/X立体リバーシ.vue`
- `frontend_web/public/X立体リバーシ/index.html`
- `frontend_web/public/X立体リバーシ/style.css`
- `frontend_web/public/X立体リバーシ/index.js`
- `frontend_web/public/Xハローワールド/index.html`
- `frontend_web/public/Xハローワールド/style.css`
- `frontend_web/public/Xハローワールド/index.js`
- `frontend_web/public/Xハローワールド/NOTICE.md`
- `frontend_web/src/components/Xその他/Xハローワールド.vue`
- `frontend_avatar/electron/main.ts`
- `frontend_avatar/electron/preload.ts`
- `frontend_avatar/src/env.d.ts`
- `frontend_avatar/src/components/AIコア.vue`
- `frontend_avatar/src/components/AIコア_xeyes.vue`
- `frontend_avatar/src/components/AIコア_xneko.vue`
- `frontend_avatar/src/components/AIコア_アバター.vue`
- `frontend_avatar/src/components/AIコア_アナログ時計.vue`
- `frontend_avatar/src/components/AIコア_デジタル時計.vue`
- `frontend_avatar/src/components/AIコア_カレンダー.vue`
- `.aidiy/knowledge/アバター表示調整とVRMA再生の連続化.md`
- `.aidiy/knowledge/X系静的画面追加.md`
- `.aidiy/knowledge/_最終変更.md`

## 確認結果

- `frontend_web/public/X立体リバーシ/index.js` で `node --check` 実行済み
- `frontend_web/public/Xハローワールド/index.js` で `node --check` 実行済み
- `frontend_web` で `npm run type-check` 実行済み
- `http://localhost:8090/X%E3%83%8F%E3%83%AD%E3%83%BC%E3%83%AF%E3%83%BC%E3%83%AB%E3%83%89/index.html` の HTTP 200 を確認済み
- `frontend_avatar` で `npm run type-check` 実行済み

## 次回の運用

- `AIコア` のアバター固有設定は `AIコア_アバター.vue` 内へ置き、`xneko(猫)` / `無し` 選択時には表示しない
- `表示選択` を増やす場合は、独立コンポーネントを作って `AIコア.vue` の import / `表示選択型` / option / 表示 computed を揃える
- Electron でウィンドウ外カーソルを追う表示を作る場合は、renderer の mousemove ではなく main process の `screen.getCursorScreenPoint()` と `BrowserWindow.getBounds()` を返す IPC を使う
- Electron で CPU 使用率連動演出を作る場合は、main process の `os.cpus()` 差分を IPC で返し、renderer 側で用途に応じた移動平均を持つ
- ファイル名リネーム後は `rg` で旧名参照を確認し、`frontend_avatar` の型チェックまで実施する
- X系静的ゲームを増やす場合は、`public/<画面名>/` の本体、`src/components/Xその他/<画面名>.vue` の iframe ラッパー、`router/index.ts`、`Xその他.vue` の4点を揃える
