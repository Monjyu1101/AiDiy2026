# 最終変更

更新日: 2026-04-15

## 今回整理した内容
- `claude` / `gemini` / `codex` がプロジェクトから利用する MCP 設定の置き場を整理した
- `claude` は `.mcp.json`、`gemini` は `.gemini/settings.json`、`codex` は `~/.codex/config.toml` または起動引数で渡す方針を知見化した
- `codex` は確認した範囲で project スコープの `mcp_servers` が効かず、起動バッチで `-c` を付ける実装が再利用しやすいと整理した
- `backend_mcp` の入口を `mcp_main.py`、再利用ロジックを `mcp_proc/` へ分離する構成に整理した
- `_start.py` の `(mcp)` ブラウザ起動ヘルパーに残っていた旧 import を `mcp_proc.chrome_manager` へ揃えた
- `scripts/cli_bat/_codex_cli_AiDiy.bat` の `-c` 上書きを外し、引用符崩れを避ける形に寄せた
- Windows で `codex.cmd -c "mcp_servers...='http://...'"` が引用符崩れで起動失敗するケースを知見化した
- Codex の `url = ...` は streamable HTTP 用で、`backend_mcp` の SSE エンドポイントは直接扱えないと確認した
- `_setup.py` の `backend_mcp` 用 MCP 設定書き込みは Claude / Gemini のみに寄せ、Codex の `backend_mcp` 設定は自動削除するようにした
- `_setup.py` の `backend_mcp` 用 MCP 設定書き込み前に、ルート `.gitignore` へ `.claude/` と `.gemini/` を自動追記するようにした
- `backend_mcp` の SSE 初回 `endpoint` イベントを、引用符付き文字列ではなく生の相対パスへ修正し、`/messages` と `/messages/` の両方を受けるようにした

## 今回の変更ファイル
- `.mcp.json`
- `.gemini/settings.json`
- `.gitignore`
- `_setup.py`
- `scripts/cli_bat/_claude-code_AiDiy.bat`
- `scripts/cli_bat/_codex_cli_AiDiy.bat`
- `C:\Users\admin\.codex\config.toml`
- `.aidiy/_index.md`
- `.aidiy/_最終変更.md`
- `.aidiy/Code CLIのMCP設定.md`
- `.aidiy/backend_mcp構成整理.md`
- `_start.py`

## 現時点の主要知見
- `C利用者パスワード運用.md`
  - 初期データはプレーン保存を許容しつつ、通常登録・変更はハッシュ保存に寄せる段階運用
- `新たなCLI追加時の作業手順.md`
  - CLI追加は backend 実装、モデル定義、設定JSON、frontend 設定UIを一式で揃える
- `CLIプロンプト正規化と履歴保持.md`
  - system prompt は保持時と送信時で扱いを分け、履歴可読性と CLI 安全性を両立する
- `Code CLIのMCP設定.md`
  - `claude` / `gemini` / `codex` で MCP 設定場所が違う
- `backend_mcp構成整理.md`
  - `backend_mcp` は `mcp_main.py` を入口、`mcp_proc/` をロジック置き場にすると整理しやすい

## 次回の運用
- 修正前に `_index.md` を確認する
- 直近の変更文脈が必要なときはこの `_最終変更.md` を先に確認する
- CLI の MCP を直すときは、`claude` / `gemini` / `codex` で設定場所が違う点を先に確認する
- `backend_mcp` の構成を直すときは、入口ファイルとロジックファイルを混在させない
- 同じテーマの修正が増えたら、既存メモへ追記統合する
