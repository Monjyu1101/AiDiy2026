# 最終変更

更新日: 2026-04-19

## 今回整理した内容
- `frontend_web/public/X自己紹介/index.html` のアバター表示を調整
  - コンテナを `200px × 340px` に抑え、キャンバスも `clientWidth/clientHeight` に追従させて見た目どおりの半サイズ表示に統一
  - `frontend_avatar` と同じくカメラを `-Z` 側へ置く構成に寄せ、正面向きになるよう調整
  - VRMA は `LoopOnce` を使って 1 本ずつ再生し、`clip.duration - XFADE` 基準のタイマーで次クリップへ巡回接続する方式へ変更

## 今回の変更ファイル
- `frontend_web/public/X自己紹介/index.html`
- `.aidiy/アバター表示調整とVRMA再生の連続化.md`
- `.aidiy/_最終変更.md`

## 現時点の主要知見
- `アバター表示調整とVRMA再生の連続化.md`
  - Vue コンポーネント（AIコア_アバター.vue）ではカメラ距離倍率で制御
  - 静的HTML（X自己紹介/index.html）では CSS コンテナ寸法と `renderer.setSize(..., false)` の同期で制御
  - 静的HTML側の向き調整は回転よりもカメラ配置を `frontend_avatar` に合わせる方が安全
  - 複数 VRMA を見せたい場合は `LoopOnce` + タイマー接続 + `crossFadeFrom` の巡回再生が扱いやすい

## 次回の運用
- 修正前に `_index.md` を確認する
- 3D表示やカメラワークの調整時は `アバター表示調整とVRMA再生の連続化.md` を参照
- 静的HTMLとVueコンポーネントで調整箇所が異なる点に注意
