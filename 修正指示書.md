# aidiy_hermes 長文実行時の WinError 206 修正指示書

## 目的

Windows 上で AiDiy から `aidiy_hermes` を実行した際、会話履歴や検証対象ファイル一覧を含む長いプロンプトによって、次のエラーが発生する問題を修正する。

```text
subprocess実行エラー: [WinError 206] ファイル名または拡張子が長すぎます。
```

このエラーは実際のファイル名の長さが原因ではない。Windows の `CreateProcess` が持つコマンドライン全体の約 32,767 文字制限を、`-z "<プロンプト>"` の引数が超えたことが原因である。

## 修正対象

別環境または別ブランチに、次の変更を同じ考え方で適用する。

- `backend_server/AIコア/AIコード_cli.py`
- `command_hermes/cli_main.py`
- `backend_server/tests/test_aidiy_hermes_stdin.py`（新規）
- `command_hermes/tests/test_cli_stdin.py`（新規）
- `command_hermes/AGENTS.md`
- `_AIDIY/knowledge/command_hermes,Windows対応規則.md`

既存コードの構成や行番号が異なる場合は、関数名と処理内容を基準に適用すること。

## 修正方針

`aidiy_hermes` に渡す完全プロンプトをコマンドライン引数へ載せず、UTF-8 の標準入力で渡す。

```text
修正前:
python cli_main.py -Q -z "<長い完全プロンプト>"

修正後:
python cli_main.py -Q --oneshot-stdin
                         ↑ 完全プロンプトは標準入力から渡す
```

短いプロンプトも同じ標準入力経路へ統一する。入力長を判定して経路を切り替える実装にはしない。

## 1. command_hermes/cli_main.py

### 1-1. CLI オプションを追加する

`_build_arg_parser()` に、標準入力からワンショット用プロンプトを読むオプションを追加する。

```python
parser.add_argument("--oneshot-stdin", action="store_true", default=False)
```

既存の `-z` / `--oneshot` は手動実行との互換性のため残す。

### 1-2. 標準入力を読み込む

`cli_entry()` の query 決定処理で、`--oneshot-stdin` 指定時は `sys.stdin.read()` を使用する。

要件:

- 入力内容を改行も含めてそのまま query にする。
- `--oneshot-stdin` は quiet・oneshot として扱う。
- 空または空白だけの入力は終了コード `2` で拒否する。
- 空入力時に対話モードへ移行させない。
- エラー説明は stderr に出力する。

実装形:

```python
query = sys.stdin.read() if args.oneshot_stdin else (
    args.oneshot or args.query or (" ".join(args.prompt) if args.prompt else None)
)
if args.oneshot_stdin and not query.strip():
    print("--oneshot-stdin requires non-empty UTF-8 input.", file=sys.stderr)
    return 2

quiet = bool(args.quiet or args.oneshot is not None or args.oneshot_stdin)
oneshot = args.oneshot is not None or args.oneshot_stdin
```

## 2. backend_server/AIコア/AIコード_cli.py

### 2-1. aidiy_hermes のコマンドからプロンプト引数を除く

`CodeAI._コマンド構築()` の `self.code_ai == "aidiy_hermes"` 分岐を変更する。

要件:

- `-z`, `プロンプト` をコマンド配列へ追加しない。
- 代わりに `--oneshot-stdin` を追加する。
- 通常モデルと `local_chat` 分岐の両方を変更する。
- Windows では引き続き `.cmd` を避け、Python と `cli_main.py` を直接起動する。

実装形:

```python
# 通常モデル
return base + ["-Q"] + model_args + ["--oneshot-stdin"]

# local_chat
return base + ["-Q"] + local_args + ["--oneshot-stdin"]
```

### 2-2. subprocess 実行メソッドへ標準入力引数を追加する

`CodeAI._subprocess実行()` に省略可能な `stdin_text` 引数を追加する。

```python
async def _subprocess実行(
    self,
    command: list,
    cwd: str,
    timeout: int,
    stdin_text: Optional[str] = None,
) -> str:
```

`stdin_text` がある場合だけ、subprocess の stdin を PIPE にする。

```python
stdin=(
    asyncio.subprocess.PIPE
    if stdin_text is not None
    else asyncio.subprocess.DEVNULL
),
```

### 2-3. 標準入力送信を他の監視処理と並行実行する

標準入力を subprocess 起動直後に同期的に `drain()` してはいけない。子プロセスが stdout/stderr を大量出力してから stdin を読む場合、親子が互いのパイプ待ちになり停止する可能性がある。

次の処理を独立した coroutine にする。

```python
async def stdin_writer():
    if stdin_text is None or process.stdin is None:
        return
    try:
        process.stdin.write(stdin_text.encode("utf-8"))
        await process.stdin.drain()
    except (BrokenPipeError, ConnectionResetError):
        pass
    finally:
        process.stdin.close()
```

stdout、stderr、タイムアウト、プロセス終了の各タスクと同時に開始する。

```python
stdin_task = (
    asyncio.create_task(stdin_writer())
    if stdin_text is not None
    else None
)
```

注意事項:

- `stdin_task` を最初の `asyncio.wait(..., FIRST_COMPLETED)` の対象には加えない。標準入力送信だけが先に完了した時点で、出力監視を終了させないためである。
- タイムアウトまたは強制停止時のキャンセル対象には `stdin_task` を含める。
- 通常終了時の残存タスク待機にも `stdin_task` を含める。
- タスクリストへ `None` を渡さない。
- Windows の ProactorEventLoop では、キャンセル中の `StreamWriter.wait_closed()` が `InvalidStateError` を発生させる場合があるため、この stdin writer では `close()` 後に `wait_closed()` を呼ばない。

### 2-4. 完全プロンプトを標準入力へ渡す

`CodeAI.実行()` から `_subprocess実行()` を呼ぶ際、`aidiy_hermes` の場合だけ完全プロンプトを渡す。

```python
result_text = await self._subprocess実行(
    command=command,
    cwd=作業ディレクトリ,
    timeout=タイムアウト秒数,
    stdin_text=完全プロンプト if self.code_ai == "aidiy_hermes" else None,
)
```

他の CLI の入力方式は変更しない。

## 3. 必須回帰テスト

### backend_server/tests/test_aidiy_hermes_stdin.py

少なくとも次をテストする。

1. `aidiy_hermes` のコマンド配列に `--oneshot-stdin` が含まれる。
2. 40,000 文字以上のプロンプト本文がコマンド配列に含まれない。
3. 40,000 文字以上の日本語入力が UTF-8 標準入力で欠落なく子プロセスへ届く。
4. 子プロセスが stdin を読む前に大量の stdout を出してもデッドロックしない。
5. 子プロセスが stdin を読まない場合も、設定時間内にタイムアウトして終了する。
6. 終了後に `current_process` が `None` へ戻る。

双方向パイプ負荷テストでは、stdin と stdout の両方に数 MB 程度を使用し、OS のパイプバッファに収まるだけの小さいデータにしない。

### command_hermes/tests/test_cli_stdin.py

少なくとも次をテストする。

1. `--oneshot-stdin` が改行を含む入力をそのまま query に渡す。
2. quiet と oneshot が両方 `True` になる。
3. 空入力では `main()` を呼ばず終了コード `2` を返す。

## 4. ドキュメント更新

### command_hermes/AGENTS.md

upstream 再移植時に消失しないよう、`cli_main.py` の AiDiy 固有差分へ「長文入力用 `--oneshot-stdin`」を追記する。

### _AIDIY/knowledge/command_hermes,Windows対応規則.md

次を記録する。

- WinError 206 は実ファイル名ではなく、長い argv でも発生する。
- `.cmd` を回避して Python を直接起動しても、CreateProcess の約 32,767 文字制限は残る。
- AiDiy 連携では完全プロンプトを `--oneshot-stdin` と UTF-8 標準入力で渡す。
- 手動実行の `-Q -z "本文"` は互換性のため維持する。

## 5. 検証コマンド

Windows の各プロジェクト用 venv で実行する。

```text
backend_server/.venv/Scripts/python.exe -m unittest discover -s backend_server/tests -v
command_hermes/.venv/Scripts/python.exe -m unittest command_hermes.tests.test_cli_stdin -v
backend_server/.venv/Scripts/python.exe -m py_compile backend_server/AIコア/AIコード_cli.py
command_hermes/.venv/Scripts/python.exe -m py_compile command_hermes/cli_main.py
command_hermes/.venv/Scripts/python.exe command_hermes/cli_main.py --version
command_hermes/.venv/Scripts/python.exe command_hermes/cli_main.py --help
```

確認基準:

- 全テストが成功する。
- `ResourceWarning`、`Task exception was never retrieved`、`InvalidStateError` が出ない。
- `--help` に `--oneshot-stdin` が表示される。
- 40,000 文字を超えるプロンプトが subprocess の command 配列へ入らない。
- 数 MB の双方向パイプ負荷でも停止しない。
- stdin を読まない子プロセスがタイムアウト後も残留しない。

## 6. 適用後の反映

`backend_server/AIコア/AIコード_cli.py` は `core_main` プロセスへ読み込まれるため、コード適用後は利用者判断で `core_main` を再起動する。

再起動前の常駐プロセスには旧コードが残る。修正確認時は、必ず再起動後の新しいログを確認すること。
