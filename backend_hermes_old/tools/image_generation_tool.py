"""FAL.ai image generation — 削除済み。

tools_config.py の import 互換のためスタブを残す。
画像生成は tools/media_tools.py (OpenAI) を使用する。
"""

FAL_MODELS: dict = {}
DEFAULT_MODEL: str = ""


def check_image_generation_requirements() -> bool:
    return False
